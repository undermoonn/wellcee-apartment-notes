{
  "name": "wellcee-apartment-notes",
  "version": "1.0.0",
  "private": true,
  "description": "A zero-build Chrome extension for private notes on Wellcee apartment listings.",
  "scripts": {
    "test": "node --check src/content.js && node --check popup/popup.js && node --test"
  },
  "engines": {
    "node": ">=18"
  }
}
